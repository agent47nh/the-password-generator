[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pytest = ">=7.0.1"
thepasswordgenerator = {editable = true, path = "."}

[dev-packages]

[requires]
python_version = "3.10"

[scripts]
test = "python -m pytest"
local_tests = "env TEST_ENV=local python -m unittest"
